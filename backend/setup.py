from setuptools import find_packages, setup

setup(
    name='hackathon-starter',
    version='0.1.0',
    packages=find_packages(),
    include_package_data=True,
    install_requires=[
        # List your project's dependencies here
        # e.g., 'requests', 'flask', etc.
    ],
)
